// app/index.tsx
import LoginScreen from '../src/screens/LoginScreen';

export default function Index() {
  // Aquí estamos llamando a la pantalla que construimos en la carpeta src
  return <LoginScreen />;
}